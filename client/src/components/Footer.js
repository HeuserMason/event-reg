import { Layout } from "antd";

function Footer() {

    return (
        <Layout
            style={{
                padding: '16px',
                position: 'fixed',
                left: 0,
                bottom: 0,
                right: 0,
                textAlign: 'center',
            }}
        >
        Event Reg ©2023 Created by Mason
      </Layout>
    );
}

export default Footer;